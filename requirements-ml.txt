# Everything the ingestion / training / scoring jobs need (GitHub Actions + local dev). Install together with requirements.txt.
requests>=2.31
python-dotenv>=1.0
pyarrow>=17
scikit-learn>=1.5
xgboost>=2.1
joblib>=1.4
tabulate>=0.9
httpx>=0.27
pytest>=8
